venv/
models/*.gguf
models/.cache/
__pycache__/
*.pyc
.DS_Store

# 外部依賴，實體放在 repo 目錄底下方便管理，但不進版控（各有數 GB）
CosyVoice/
miniconda3/

# ASR 測試語音（可用 scripts/gen_tts_samples.py 或 scripts/record_wav.py 重生）
samples/
*.wav
# demo 頁「播放範例語音」按鈕用的內建 wav，要進版控
!demo-assets/sample-zh.wav

# 匯出的報告 PDF（原始檔是 Artifact，不進版控）
*.pdf

# 本機跑 uvicorn 時的輸出，不進版控
server_out.log
server_e*.log
